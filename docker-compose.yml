services:
  app:
    build: .
    container_name: booking-badminton-api
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
